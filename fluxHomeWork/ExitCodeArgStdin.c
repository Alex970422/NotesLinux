#include <stdio.h>
#include <stdlib.h>

int main(int argc , char *argv[]){
char *mess;
mess = (char*)malloc(100 * sizeof(char));
printf("Entrer votre mot\n");
scanf("%s",mess);
 if (argv[1] = mess)
 exit(1);
 else 
 exit(0);
return 0;
}
